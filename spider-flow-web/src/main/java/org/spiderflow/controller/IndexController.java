package org.spiderflow.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class IndexController {
    
    // 由ResourcesConfiguration处理根路径的重定向
    // 这里不再需要处理根路径
}